[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "email-spam-classifier"
version = "0.1.0"

[tool.uv]
# Keep this minimal; we're using a lightweight, pure-Python runtime for Vercel builds.
extra-build-dependencies = {}
